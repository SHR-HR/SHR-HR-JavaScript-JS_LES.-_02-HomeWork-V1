// Решение квадратного уравнения (формула с использованием Math.sqrt):
// 1. Ввод значений a, b, c.
// 2. Вычисление дискриминанта (D):
//    D = b^2 - 4ac
// 3. Решение уравнения в зависимости от значения дискриминанта:
//    Если D > 0, то два корня:
//       x1 = (-b + √D) / (2a)
//       x2 = (-b - √D) / (2a)
//    Если D = 0, то один корень:
//       x = -b / (2a)
//    Если D < 0, корней нет (решений в действительных числах нет).

// Функция для решения квадратного уравнения

function solveQuadratic(a, b, c) {
    console.log(`Решаем уравнение: (${a})x^2 + (${b})x + (${c}) = 0`);
// Функция solveQuadratic:
// Принимает три параметра 𝑎, 𝑏,𝑐.
// Вычисляет дискриминант и решает уравнение в зависимости от значения дискриминанта.
// Результаты выводятся в консоль.


    // Вычисляем дискриминант

    const D = b ** 2 - 4 * a * c;
    console.log(`Дискриминант (D): ${D}`);

    // Проверяем дискриминант

    if (D > 0) {
        const x1 = (-b + Math.sqrt(D)) / (2 * a);
        const x2 = (-b - Math.sqrt(D)) / (2 * a);
        console.log(`Уравнение имеет два корня: x1 = ${x1}, x2 = ${x2}`);
    } else if (D === 0) {
        const x = -b / (2 * a);
        console.log(`Уравнение имеет один корень: x = ${x}`);
    } else {
        console.log("Уравнение не имеет действительных корней.");
    }
}

// Пример использования функции

const a = 1; // Коэффициент при x^2
const b = -3; // Коэффициент при x
const c = 2; // Свободный член

solveQuadratic(a, b, c);

// Вывод:
// При 𝑎=1, b=−3, c=2 то результат будет:
// Решаем уравнение: (1)x^2 + (-3)x + (2) = 0
// Дискриминант (D): 1
// Уравнение имеет два корня: x1 = 2, x2 = 1
